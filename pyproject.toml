[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "privcheck"
version = "0.1.0"
description = "Read-only Unix permission analysis helpers"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["privcheck"]
